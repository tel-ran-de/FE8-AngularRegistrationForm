import { TransDirective } from './trans.directive';

describe('TransDirective', () => {
  it('should create an instance', () => {
    const directive = new TransDirective();
    expect(directive).toBeTruthy();
  });
});
